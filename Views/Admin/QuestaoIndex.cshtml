@model IEnumerable<_30Code.Models.Questoes>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutPageAdmin.cshtml";
}
<style>
    .page-wrapper {
        background-color: white;
    }
</style>

<div class="page-wrapper mt-2">
    <div class="container">
        <h2>Questões</h2>
        <p>
            @Html.ActionLink("Criar novo", "QuestaoCreate")
        </p>
        <form class="row" asp-controller="Questoes" asp-action="Index" method="post">
            <div class="col-6">
                <div class="input-group input-group-sm mb-3">
                    <div class="input-group-prepend">
                        <span class="input-group-text" id="inputGroup-sizing-sm">Procurar</span>
                    </div>
                    <input type="text" class="form-control" aria-label="Small" aria-describedby="inputGroup-sizing-sm" name="txtProcurar">
                </div>
            </div>
            <div class="col-6">
                <button type="submit" class="btn-sm btn-success">Procurar</button>
            </div>
        </form>

        <div class="table-responsive">
            <table id="zero_config" class="table table-striped table-bordered">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Conteudo.Titulo)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Titulo)
                    </th>
                    <th></th>
                </tr>

                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Conteudo.Titulo)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Titulo)
                        </td>
                        <td>
                            @Html.ActionLink("Editar", "QuestaoEdit", new { id = item.Id }) |
                            @Html.ActionLink("Detalhes", "QuestaoDetails", new { id = item.Id }) |
                            @Html.ActionLink("Deletar", "QuestaoDelete", new { id = item.Id })
                        </td>
                    </tr>
                }
            </table>
        </div>
    </div>
</div>