@model _30Code.Models.Usuario_has_curso

@{ ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_LayoutPageAdmin.cshtml";
}

<style>
    .page-wrapper {
        background-color: white;
    }
</style>
<div class="page-wrapper mt-2">
    <div class="container">
        <h2>Deletar</h2>

        <h3>Tem certeza que deseja deletar esse usuário/curso?</h3>
        <div>
            <hr />
            <dl class="dl-horizontal">
                <dt>
                    @Html.DisplayNameFor(model => model.Curso.Nome)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Curso.Nome)
                </dd>

                <dt>
                    @Html.DisplayNameFor(model => model.Usuario.Nome)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Usuario.Nome)
                </dd>

            </dl>

            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()

                <div class="form-actions no-color">
                    <input type="submit" value="Deletar" class="btn btn-default" /> |
                    @Html.ActionLink("Voltar para Home", "Index")
                </div>
            }
        </div>
    </div>
</div>